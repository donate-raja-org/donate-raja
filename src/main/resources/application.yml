server:
  port: 8080


management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
spring:
  application:
    name: DonateRaja
    description: A platform for donations, renting, and receiving items.
    version: 1.0.0
  datasource:
    url: jdbc:postgresql://ep-tiny-forest-a17k2xt8.ap-southeast-1.aws.neon.tech:5432/donateraja
    username: donateraja_owner
    password: 9iTbhtqjEkg2
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    user:
      name: admin
#      password: "Admin@123"
      password: "$2a$10$WJ9y7iQbh3GpFEry4OsUzHJk.y5o/4YOxOB1elH8XyDg.TgYw9bqG"   # Replace with your encoded password
logging:
  level:
    org.springframework: INFO
    com.donate.raja.donate_raja: DEBUG  # Adjust log level for your classes
  file:
    name: logs/app.log  # Log to a specific file
